# SQLite database file, relative to the project root.
DATABASE_URL="file:./dev.db"

# Optional: override the seeded admin account (see prisma/seed.ts).
# ADMIN_SEED_EMAIL="owner@example.com"
# ADMIN_SEED_PASSWORD="change-me"
